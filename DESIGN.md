# 학급 안내 앱 — 개인 공지 시스템 설계문서

> 대상: `morningcandy/class-notice` (GitHub Pages, 공개 정적 사이트)
> 범위: 이번 작업에서 구현한 **개인 코드 로그인 · 개인/전체 공지 구분 · 체크리스트** 부분.
> 학사일정 달력(`calendarEvents`/`holidays`/`specialRanges`)은 이 문서 범위 밖(기존 유지).
> 이 문서에는 **실명·전화번호·개인 코드 원본을 절대 포함하지 않는다.**

---
## ★ 현재 아키텍처 (v2, 2026-08-15 — 서버 문지기로 이관 완료)

개인 공지의 **실질 비공개**를 위해 데이터 소스를 **비공개 구글시트 + Apps Script 웹앱(문지기)**로 이관했다. 이 v2가 **현재 유효 설계**이며, 아래 §2~§7의 codeHash 방식은 **폐기(superseded)**된 v1 기록이다.

- **저장소**: 공지/할일/명단(코드)/개인업무 = 비공개 구글시트 "2-3반 관리 마스터"(교사 계정). 공개 repo·`data.js`에는 **개인 공지 본문·코드가 없다**(달력 데이터만 남김).
- **문지기**: Apps Script 웹앱(`apps-script.gs`, 배포=모든 사용자/실행=교사). API: `GET|POST ?code=<코드>` → `{ok, student:{num,name:"N번"}|null, notices[], tasks[]}`. 코드가 맞아야 본인 항목을 받고, **이름·코드·명단·개인업무는 절대 반환하지 않는다** → 파일을 뜯어도 남의 개인 공지를 못 본다(v1 대비 실질 강화).
- **클라이언트**(`index.html`): `SHEET_API`로 fetch. 로그인=코드 입력→`loadData(code)`→서버가 필터한 `notices/tasks` 렌더. 실패는 `showConnError()`로 화면에 표시(무음/가짜데이터 금지). 코드는 `localStorage(MYCODE_KEY)`에만(저민감), 명단·해시는 클라이언트에 없음.
- **검증(2026-08-15, Playwright 라이브)**: 13/13 통과 — 초기 전체 로드, 7번 로그인(개인 표시), 1번 로그인(7번 개인 미표시), 무효 코드 오류, JS 무오류. 백엔드 응답·CORS GET도 실측 확인.
- **폐기물**: `admin.html`(로컬)의 codeHash 생성·data.js 줄 생성 기능은 v2에서 불필요(내용은 구글시트에서 직접 편집). 교사 작성 워크플로 = **구글시트 탭 편집**.

---
## 1. 목적과 도메인 규칙  *(이하 §1~§7은 v1 codeHash 방식 — 폐기, 기록용)*

- 하나의 공개 페이지에서, **전체 공지**는 누구나 보고 **개인 공지**는 본인만 확인한다.
- 공개 저장소(GitHub Pages)에는 **개인정보 원본(실명·학번·전화번호·생년월일)을 두지 않는다.**
- 도메인 규칙: "학생 식별은 **각 학생만 아는 값**으로 하되, 그 값 자체를 공개 파일에 평문으로 남기지 않는다."

## 2. 아키텍처 (공개/비공개 경계)

| 구성요소 | 위치 | 공개 여부 | 역할 |
|---|---|---|---|
| `index.html` | repo | 공개 | 뷰어 + 로그인/필터 로직 |
| `data.js` | repo | 공개 | 익명 명단(`students`) + `notices`/`tasks` + 달력 데이터 |
| `admin.html` | 로컬(내 PC) | **비공개(`.gitignore`)** | 실명·전화번호 대조표 + 공지 작성기 |
| `.gitignore` | repo | 공개 | `admin.html`, `*.local.js`, `*.xlsx`, `*.pdf` 차단 |

경계 규칙: **개인정보가 담긴 산출물은 repo 밖 또는 `.gitignore`에만 존재한다.** 공개 파일은 익명 데이터와 단방향 변환값만 포함한다.

## 3. 데이터 모델 (`data.js`)

```
students: [ { id, num, name, codeHash } ]      // name = "N번 ㄱㅁㅎ"(익명), 실명 없음
notices:  [ { id, title, content, date, audience, urgent } ]
tasks:    [ { id, title, dueDate, audience } ]
```

- `id === num` (1..28). `name`은 번호+자음 이니셜만.
- `audience`: `'all'`(전체) 또는 학생 `id`(개인). 개인 공지는 로그인한 그 학생에게만 보인다.
- `codeHash`: 아래 개인 코드의 단방향 변환값. **원본 코드는 저장하지 않는다.**

## 4. 개인 코드 체계

- 코드 = **번호(2자리) + 휴대폰 뒤 4자리** (예: 1번·폰뒤 9596 → `019596`).
- 유일성: 번호가 유일 키이므로 코드도 유일. (생년월일은 중복 사례가 있어 채택하지 않음.)
- 정규화 `normalizeCode(raw)`: 숫자만 남기고 왼쪽을 `0`으로 채워 6자리. → `"19596"`도 `"019596"`로 인식.
- 변환 `hashCode(code)`: djb2 계열. `h = 5381; h = (h*33 + charCode) % 2^32` 반복, salt `"hs2026:"` 접두. 결과는 16진수 문자열. 중간값이 항상 `< 2^53`이라 부동소수점 정확도 손실 없음.
- 공개 `data.js`에는 `codeHash`만, `admin.html`(비공개)에는 원본 대조표만 존재한다. **두 곳의 salt·알고리즘은 반드시 동일해야 한다.**

## 5. 로그인/필터 흐름 (`index.html`)

```
학생 입력(raw)
  → normalizeCode → hashCode
  → students.find(s => s.codeHash === h)
     ├─ 일치: currentStudent 설정, localStorage(MYID_KEY) 저장
     │         → notices/tasks를 audience==='all' || audience===currentStudent.id 로 필터
     └─ 불일치: "코드가 올바르지 않아요" 오류 메시지(무음 실패 아님)
```

- 세션 유지: `localStorage`에 학생 `id`만 저장(코드/해시는 저장하지 않음). 재방문 시 복원.
- 체크리스트: 학생별 완료 상태는 `localStorage`(해당 기기 한정) + 선택적으로 Google Form에 기록.

## 6. 관리자 도구 (`admin.html`, 비공개)

- 인라인 `roster`에 번호·실명·학번·폰뒤4·생일·비고 보유(내 PC 전용).
- 기능 ① 대조표(번호↔실명↔코드↔codeHash) ② 공지/할일 작성기(대상 다중 선택 → `notices`/`tasks` 줄 생성·복사) ③ `students` 배열 재생성.
- `hashCode`는 `index.html`과 동일 구현이어야 함(계약).

## 7. 프라이버시 속성과 한계 (명시적 고지)

- **보장**: 공개 repo에 실명·전화번호·생년월일 없음. `admin.html`은 `.gitignore`로 업로드 차단.
- **한계(설계상 수용)**:
  - 개인 공지 **본문 자체는** 공개 `data.js`에 존재 → 파일을 직접 열면 로그인 없이도 읽을 수 있다. 즉 코드 로그인은 **UI 필터**이지 접근통제가 아니다.
  - 코드는 6자리(저엔트로피)라 `codeHash`는 **평문 노출 방지·가벼운 자물쇠** 수준이며 무차별 대입에 강하지 않다.
  - 따라서 **민감도 낮은 안내에 한해 적합**하다(합의된 사용 범위).
- 향후: 개인 공지의 실질적 비공개가 필요하면 §8의 서버 게이트웨이로 이관한다.

## 8. 향후 방향 (제안 — 미구현)

- 마스터 데이터를 **비공개 Google Sheet**로 이관, **Apps Script 웹앱**이 코드로 본인 것만 반환.
- 이때 개인 공지 본문·코드가 공개 파일에서 사라져 실질적 비공개가 성립.
- 학사일정 외 `notices`/`tasks`가 시트에서 자동 갱신되어 매 배포 push 불필요.
- 이 문서의 §3~§7은 그 이관 시 서버 응답 계약으로 재정의된다.

## 9. 수용 기준 (요구사항 → 근거)

| 요구사항 | 근거(코드) | 상태 |
|---|---|---|
| 번호+폰뒤4자리로 로그인 | `normalizeCode`/`hashCode`/`submitCode` | 구현됨 |
| 개인 공지는 본인만 표시 | `notices/tasks` `audience` 필터 | 구현됨(기존+로그인 결합) |
| 공개 repo에 개인정보 원본 없음 | `data.js`=codeHash만, `admin.html`=gitignore | 구현됨 |
| 잘못된 코드는 명시적 실패 | `submitCode` 오류 메시지 | 구현됨 |
| 실제 실행 화면 QA | Playwright 7/7 PASS(§10) | ✅ 검증됨 |

## 10. 검증 상태 (실행 표면 QA 완료)

- **정적 검토**: 스크립트 로드 순서(`data.js`→`index.html`), `getElementById`↔`id` 대응, 해시 알고리즘 동치(awk 계산값 = JS 구현) 확인.
- **실행 QA (Playwright + 헤드리스 Chromium)**: 편집된 워킹트리 `index.html`+`data.js`를 실제 브라우저로 실행해 검증. **7/7 통과**:
  - A 유효코드 `019596` → "1번 ㄱㅁㅎ" 로그인
  - B 정규화 `19596`(앞 0 생략) → "1번 ㄱㅁㅎ"
  - C 유효코드 `108465` → "10번 ㅅㅇㅅ"
  - D 무효코드 `000000` → 오류 메시지 + 로그인 안 됨
  - E1 로그인 전 개인공지 숨김 / E2 1번 로그인 시 본인 개인공지만 표시(7번 것 안 보임)
  - JS 콘솔/페이지 오류 없음
- **QA 실행 환경**: 이 세션 스크래치패드에 포터블 Node.js(v24 LTS) + Playwright + Chromium을 설치해 수행. (관리자 권한 불필요, repo 미오염.)
- **남은 것**: 위 QA는 **로컬 편집본**을 검증한 것. **배포본(live)은 아직 옛 `data.js`(code:)** 상태이므로, `git push` 후 배포본에서 동일 확인(또는 재실행) 필요.
